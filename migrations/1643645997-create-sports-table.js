exports.up = async function up(sql) {
  await sql`
	CREATE TABLE sports (id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, sportname varchar(40) NOT NULL);
	`;
};

exports.down = async function down(sql) {
  await sql`
DROP TABLE sports;
`;
};
